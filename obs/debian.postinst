#!/bin/sh -e
#
app_name=ngrt4n
app_home=/var/lib/${app_name}
useradd ${app_name} -s /bin/false -d ${app_home}
chmod 664 ${app_home}/db
chown -R ${app_name}:${app_name} ${app_home} 

chown ${app_name}:${app_name} /usr/bin/${app_name} && chmod g+s /usr/bin/${app_name}
#DEBHELPER#
